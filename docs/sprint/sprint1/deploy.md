# Sprint 1 배포 체크리스트

> Sprint 1: UI 뼈대 구축 (Phase 1 완료)
> 작성일: 2026-03-13

---

## 자동 검증 완료 항목

### 빌드 검증

- ✅ `npm run build` 성공 (Next.js 16.1.6 Turbopack)
  - 컴파일 성공: 2.5초
  - TypeScript 검사 통과
  - 정적 페이지 생성 완료 (4/4)
  - 빌드 경고 없음

### 코드 품질

- ✅ TypeScript 타입 검사 통과 (빌드 내 포함)
- ✅ Next.js App Router 클라이언트 컴포넌트 경계 정상 설정
- ✅ `"use client"` 선언 필요한 컴포넌트에 올바르게 적용

---

## 수동 검증 필요 항목

### UI 기능 검증 (npm run dev 실행 후 브라우저에서 직접 확인)

> `npm run dev` 실행 명령어: `C:\Users\hsyun\AppData\Local\nvm\v22.14.0\node.exe node_modules\next\dist\bin\next dev`

- ⬜ `http://localhost:3000` 접속 시 레이아웃이 정상 렌더링됨
  - 네비게이션 바에 "Sorting Algorithm Visualizer" 타이틀 표시
  - 알고리즘 드롭다운 (버블/선택/삽입/퀵/병합 정렬 5가지 옵션)
  - 배열 크기 슬라이더 (기본값 50 표시)
  - 속도 슬라이더
  - 막대 그래프 50개 렌더링
  - 하단 컨트롤 바 (새 배열 생성, 정렬 시작 버튼)

- ⬜ "새 배열 생성" 버튼 클릭 시 새 랜덤 막대 그래프가 표시됨

- ⬜ 배열 크기 슬라이더 조작 시 막대 개수가 실시간 변경됨
  - 최소값(10)으로 내리면 막대 10개 표시
  - 최대값(100)으로 올리면 막대 100개 표시

- ⬜ 브라우저 콘솔(F12 → Console)에 에러/경고 없음

### 반응형 레이아웃 검증 (브라우저 개발자 도구에서 뷰포트 변경)

- ⬜ 모바일 뷰포트 (375px × 812px)
  - 네비게이션 컨트롤이 세로 배치
  - 막대 그래프가 화면 너비에 맞게 표시
  - 버튼이 세로 배치

- ⬜ 태블릿 뷰포트 (768px × 1024px)
  - 컨트롤이 가로 배치로 전환

- ⬜ 데스크톱 뷰포트 (1440px × 900px)
  - 전체 가로 배치 레이아웃 확인

### 시각적 디자인 확인

- ⬜ 다크 배경(gray-950)에서 파란색(blue-500) 막대가 명확하게 구분됨
- ⬜ "정렬 시작" 버튼이 비활성화 상태(disabled)에서 시각적으로 구분됨 (Phase 3 구현 후 isSorting=true 상태 테스트)
- ⬜ 슬라이더 색상(accent-blue-500)이 파란색으로 표시됨

### 배포 (타이밍은 사용자가 결정)

- ⬜ Vercel 배포
  - GitHub 저장소 Vercel 연결
  - 자동 배포 설정 확인
  - 배포 URL에서 프로덕션 동작 확인

---

## 참고

- Playwright MCP 자동 검증은 `npm run dev` 실행 중일 때 sprint-close 시점에 실행 가능
- 현재 Sprint 1 마무리 시점에 개발 서버가 실행 중이지 않아 Playwright 검증은 수동으로 대체
- Sprint 2 마무리 시 Playwright 자동 검증 재시도 권장
